*** Settings ***
Library     SSHLibrary
Resource    ../../resources/settings.txt    

*** Variables ***
${syncTBEJobs}    ./manage.py --sync sync-taleo-requisitions

*** Keywords ***
Open connection and log in
  [Documentation]  SSH into the specified host with given credentials
    Open Connection   ${HOST_STAGING}
    Login With Public Key  ${USERNAME_STAGING}  ${KEYFILE}  ${PASSWORD_STAGING}
    
Run Sync Jobs to Taleo
  [Documentation]  Logs into staging environment and runs manage.py command to sync taleo requisitions 
    Open connection and log in
    Write  cd current   
    Write  source virtualenv/bin/activate 
    Write  ${syncTBEJobs}
